# -----------------------------------------------------------------
# Configuration file with thridparty gmp
# -----------------------------------------------------------------

set (GMP_ROOT        "${THIRDPARTY_DIRECTORY}/gmp/gmp.install")
set (GMP_INCLUDE_DIR "${GMP_ROOT}/include")
set (GMP_LIBRARIES
    "${GMP_ROOT}/gmp.lib"
    )
